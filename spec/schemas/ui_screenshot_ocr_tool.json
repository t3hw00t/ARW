{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://arw/spec/schemas/ui_screenshot_ocr_tool.json",
  "title": "ui.screenshot.ocr",
  "type": "object",
  "properties": {
    "path": { "type": "string" },
    "lang": {
      "type": "string",
      "description": "Optional tessdata language code; defaults to 'eng' when omitted"
    },
    "force": {
      "type": "boolean",
      "description": "Set true to bypass cached OCR sidecars and recompute"
    }
  },
  "required": ["path"]
}
