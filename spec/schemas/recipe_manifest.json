{
  "$comment": "SPDX-License-Identifier: MIT OR Apache-2.0",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/t3hw00t/ARW/spec/schemas/recipe_manifest.json",
  "title": "AgentRecipeManifest",
  "type": "object",
  "required": ["id", "name", "version", "prompts", "tools", "permissions"],
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-_.]{0,63}$" },
    "name": { "type": "string", "minLength": 1 },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
    },
    "model": {
      "type": "object",
      "properties": {
        "preferred": { "type": "string", "minLength": 1 },
        "fallback": { "type": ["string", "null"], "default": null, "minLength": 1 }
      },
      "additionalProperties": false,
      "required": ["preferred"]
    },
    "permissions": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[a-z][a-z0-9._:-]{0,63}$" },
      "additionalProperties": {
        "type": ["string", "object"],
        "oneOf": [
          { "enum": ["ask", "allow", "never"] },
          {
            "type": "object",
            "properties": {
              "mode": { "enum": ["ask", "allow", "never"] },
              "ttl_secs": { "type": "integer", "minimum": 0 }
            },
            "required": ["mode"],
            "additionalProperties": false
          }
        ]
      }
    },
    "ui": {
      "type": "object",
      "properties": {
        "form_from_schema": { "type": "boolean", "default": true },
        "review_required": { "type": "boolean", "default": true }
      },
      "additionalProperties": false
    },
    "prompts": {
      "type": "object",
      "properties": {
        "system": { "type": "string", "minLength": 1 },
        "user_template": { "type": ["string", "null"], "default": null }
      },
      "required": ["system"],
      "additionalProperties": false
    },
    "tools": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "minLength": 1, "pattern": "^[a-z0-9][a-z0-9-_.:]*$" },
          "params": { "type": ["object", "null"], "default": null }
        },
        "required": ["id"],
        "additionalProperties": false
      },
      "minItems": 1
    },
    "workflows": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "object",
        "properties": {
          "step": { "type": "string", "minLength": 1 },
          "tool": { "type": "string", "minLength": 1 },
          "params": { "type": ["object", "null"], "default": null }
        },
        "required": ["step", "tool"],
        "additionalProperties": false
      },
      "default": []
    },
    "tags": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true,
      "default": []
    },
    "notes": { "type": ["string", "null"], "default": null, "minLength": 1 }
  },
  "additionalProperties": false
}
