name: CodeQL

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  schedule:
    - cron: '0 3 * * 1'

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze (CodeQL)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04]
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - name: Install Tauri (WebKitGTK) deps (Linux)
        run: |
          sudo apt-get update
          # GTK/WebKitGTK and related deps for tauri/wry (Linux)
          sudo apt-get install -y \
            pkg-config \
            build-essential \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev
          # WebKitGTK API version varies by Ubuntu image; try 4.1 then 4.0
          sudo apt-get install -y libwebkit2gtk-4.1-dev || \
          sudo apt-get install -y libwebkit2gtk-4.0-dev
      - name: Rust toolchain
        uses: dtolnay/rust-toolchain@5d458579430fc14a04a08a1e7d3694f545e91ce6 # stable
      - name: Initialize CodeQL
        uses: github/codeql-action/init@b97083e1835df46b2227bae5c4fc6e43569531c1 # v3
        with:
          languages: rust
      - name: Build (workspace)
        run: |
          cargo build --workspace --locked
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@b97083e1835df46b2227bae5c4fc6e43569531c1 # v3
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
