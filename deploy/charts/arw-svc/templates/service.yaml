apiVersion: v1
kind: Service
metadata:
  name: {{ include "arw-svc.fullname" . }}
  labels:
    {{- include "arw-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arw-svc.selectorLabels" . | nindent 4 }}

